// Minimal ESM fixture module for scripts/__tests__/import-file.test.js --
// proves importFile() can dynamically import a real file on disk via a
// pathToFileURL-derived specifier, on whatever platform the test runs on.
export const IMPORT_FILE_FIXTURE_MARKER = 'import-file-fixture-ok';
